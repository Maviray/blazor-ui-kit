@page "/PageIconsMini"
@inherits SamplePageBase

<PageTitle>Icons Mini</PageTitle>

<div class="container mx-auto p-6 flex flex-col gap-2">
    <h1 class="text-3xl font-bold mb-6">Icons Mini</h1>
    
    <div>
        <p class="@CssManager.Combine(Tailwind.Theme.Typography.BODY_MEDIUM, "text-(--theme-dark-six)")">
           Example Usage
        </p>
        <pre class="mt-2 rounded-xl border border-slate-200 bg-slate-50 p-3 text-xs">
        <code>  
&lt;MaviIconMini IconMiniType="IconMiniType.ArrowDownMini" IconColor="IconColor.DimGrey" IconSize="IconSize.Medium" OnClick="ProvideDelegate" &gt;&lt;/MaviIconMini&gt;
        </code>
     </pre>
    </div>

    <div class="grid gap-4" style="grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));">
        @foreach (var icon in Enum.GetValues<IconMiniType>())
        {
            <div class="flex flex-col justify-center items-center p-4 border rounded-lg hover:shadow-lg transition-shadow bg-white min-h-[140px]">
                <MaviIconMini IconMiniType="@icon" IconColor="IconColor.DimGrey" IconSize="IconSize.Medium"></MaviIconMini>
                <span class="mt-2 text-sm text-center text-gray-700 w-full px-1">@icon</span>
            </div>
        }
    </div>
</div>